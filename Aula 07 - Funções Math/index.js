
//OBS: Altere os valores de x para testar as funções math.
let x = -3.14;
let y = 5;
let z = 9;
let maximum;
let minimum;

// Funções Math
// x = Math.round(x); // Arredonda para o valor mais próximo
// x = Math.floor(x); // Arredonda para o valor mais baixo
// x = Math.ceil(x); // Arredonda para o valor mais alto
// x = Math.pow(x, 2); // Potenciação (nesse caso - x^2)
// x = Math.sqrt(x); // Raiz quadrada
// x = Math.abs(x); // Imprime o valor absoluto da variavel
// console.log(x)

// Funções que analizam os valores máximos e minimos entre outros valores/variaveis
maximum = Math.max(x, y, z);
console.log(maximum);

minimum = Math.min(x, y, z);
console.log(minimum);

// Função para imprimir o PI
a = Math.PI;
console.log(a);